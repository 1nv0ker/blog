interface:
  display_name: "Sanity Blog Publish"
  short_description: "Validate and safely publish a new blog post"
  default_prompt: "Use $sanity-blog-publish to prepare, validate, and publish a new blog post safely."
policy:
  allow_implicit_invocation: false
