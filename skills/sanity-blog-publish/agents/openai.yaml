interface:
  display_name: "Sanity Blog Publish"
  short_description: "Publish a templated blog with structured media"
  default_prompt: "Use $sanity-blog-publish to select a template, create structured bilingual content and warranted media, preview it, and publish safely."
policy:
  allow_implicit_invocation: false
