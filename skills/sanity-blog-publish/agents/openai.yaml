interface:
  display_name: "Sanity Blog Publish"
  short_description: "Publish a blog with full SEO and optional AI images"
  default_prompt: "Use $sanity-blog-publish to prepare, validate, preview, and safely publish a bilingual blog post with complete SEO and only the body images its content needs."
policy:
  allow_implicit_invocation: false
