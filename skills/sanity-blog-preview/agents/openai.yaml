interface:
  display_name: "Sanity Blog Preview"
  short_description: "Preview templated blog layouts and rich media"
  default_prompt: "Use $sanity-blog-preview to select a template, draft structured bilingual content and warranted media, and render a safe local preview."
policy:
  allow_implicit_invocation: false
